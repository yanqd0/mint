# T11 state change (reopen/blocked/drop)

**Purpose**: Record why a state changed.

```markdown
**Change** <reopen/blocked/drop>
**Reason** <one sentence>
```

Drop reason can merge into dropped_reason.
