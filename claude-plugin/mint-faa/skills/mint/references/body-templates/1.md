# T1 bug (problem)

**Purpose**: Record a bug/problem.

```markdown
**Symptom** <one sentence>
**Location** <path/command/trigger>
```

Location covers repro/trigger; add `? TODO confirm` tail when expectation is unclear.
